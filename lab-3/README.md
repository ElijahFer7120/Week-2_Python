# REFLECTIONS

it is an input loop program that has an exit. it only stops if i type in "HENSHIN" into the terminal which then it stops. anything other than "HENSHIN" would loop it back to the start. 